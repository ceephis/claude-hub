# Session Notes — 2026-04-12

## Changes Made

### 1. `claude --continue` for all project buttons
**File:** `lib/hub_web/live/dashboard_live.ex` line ~225
**Change:** `claude` → `claude --continue` in the AppleScript command
**Effect:** Every "Open in Claude" button now resumes the last conversation for that project instead of starting a fresh one.
**Revert:** Change `claude --continue` back to `claude` in the `write text` line.

---

### 2. Full cards for direct projects of intermediate groups
**File:** `lib/hub_web/components/group_accordion.ex`
**Problem:** Groups that have both direct projects AND subgroups (e.g. Transcendence Studios with Books subgroup) were only showing mini cards for the nested subgroup projects — direct projects were counted but invisible.
**Change:** In the intermediate group template, added a full-card grid for `@visible_cards` (direct projects) before the mini card overview and subgroup rows.
**Revert:** Remove the `<%-- Direct full cards (if any) --%>` block from the intermediate level section in `group_node`.

---

### 3. Weekly report falls back to file mtime
**File:** `lib/hub/weekly_report.ex`
**Problem:** Weekly report only showed projects with git commits to STATUS.md that week — book projects with no git remote/history never appeared.
**Change:** Added `status_mtime_in_range?/3` helper. `status_changed_in_git?` now checks git first, then falls back to STATUS.md file modification time.
**Status:** Deployed but result not yet confirmed working in the UI — needs follow-up in the morning.
**Revert:** Remove the `|| status_mtime_in_range?(...)` call and delete the `status_mtime_in_range?/3` function.

---

### 4. Book project STATUS.md updates
**Projects updated:** Atomnus, Reflectare, Invasion5, Bermuda, Qlink, Relative, Wet, Dying, Negligible9, Short
**Changes:**
- Status: 🟡 Planning → 🟢 Active (all 10)
- Stack: "TBD" / "Literary fiction" → "Science fiction" (Atomnus, Reflectare, Invasion5, Bermuda, Qlink, Relative)
**Revert:** Manually edit each STATUS.md back to 🟡 Planning / original stack if needed.

---

## Outstanding / Follow Up in the Morning
- Weekly report mtime fallback (#3) — confirm book projects now appear in the report
- If still not working, may need to investigate the report date range or how `until` is calculated
- Git backup setup for book projects (discussed but deferred)
